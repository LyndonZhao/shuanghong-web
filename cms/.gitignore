node_modules/
.tmp/
.strapi/
.strapi-updater.json
types/
exports/
.cache/
build/
.env
.env.local
.env.*.local

# DB lives in .cms/db/data.db and is tracked as part of source
# (intentional: deployments carry the latest content state)

